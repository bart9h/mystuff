list files provided by a (possibly uninstalled) package:
(after installing apt-file, it requires a lengthy `apt-file update` before it can do it's magic)

	apt-file list <package-name>

list what (possibly uninstalled) package(s) provide a file:

	apt-file search <file>
